namespace hodnota
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Zadej hodnotu");
            int hodnota = int.Parse(Console.ReadLine());
            if ((hodnota >= 0) && (hodnota <= 20)) ;
            {
                if (hodnota <= 10)
                    Console.WriteLine("Hodnota je v 1. intervalu");
                else
                    Console.WriteLine("Hodnota je 
                }
            else
            {
                Console.WriteLine("Zadána nesprávná hodnota" +  );
            
}
