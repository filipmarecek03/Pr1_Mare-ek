using System.Threading.Channels;

namespace if_else
{
    internal class Program
    {
        static void Main(string[] args)
        {
            double vysledek =;
            Console.WriteLine("Zadej hodnotu k odmocneni: ");
            int hodnota = int.Parse(Console.ReadLine());
            double odm = Math.Sqrt(cislo);
            if hodnota > 0;
            {
                vysledek = Math.Sqrt(hodnota);
                Console.WriteLine("odmodćnina je :" + vysledek);

                   
            }
            else
            {
                Console.WriteLine 
            }
    }
}
